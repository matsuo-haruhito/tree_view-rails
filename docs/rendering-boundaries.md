# Rendering boundaries

TreeView relies on Rails view helpers and normal ERB output for rendered values.

Documented builders should return simple values such as class names, data attributes, labels, and JSON-serializable payload hashes.

Application-specific validation remains in the host Rails application.
